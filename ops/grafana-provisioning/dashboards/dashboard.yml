apiVersion: 1

providers:
  - name: email-warmup
    orgId: 1
    folder: Email Warmup
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
